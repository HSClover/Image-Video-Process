# .gitignore

# ----------------------------------------------------
# 1. 윈도우즈 및 OS 관련 자동 생성 파일 제외
# ----------------------------------------------------
# Windows 썸네일 캐시 파일
Thumbs.db
# macOS/Linux에서 생성되는 메타데이터 파일 (Windows 사용 시 불필요하나 포함 권장)
.DS_Store

# ----------------------------------------------------
# 2. Python 환경 자동 생성 파일 제외
# ----------------------------------------------------
# Python 컴파일된 바이트코드 폴더와 파일
__pycache__/
*.pyc

# Virtual Environments (가상 환경 폴더)
venv/
env/

# IDE 설정 및 빌드 폴더 (예: VS Code, PyCharm)
.vscode/
.idea/
/build/
/dist/

# ----------------------------------------------------
# 3. 대용량 데이터 및 프로젝트 결과물 제외 (핵심)
# ----------------------------------------------------

# ⭐⭐ 훼손 및 블러링 데이터 폴더 전체 제외 ⭐⭐
훼손 및 블러링 데이터 폴더/

# 학습된 모델 가중치 파일 제외 (대용량 파일)
*.pth
*.pt
*.onnx
*.h5

# 학습 로그 및 최종 결과 이미지/영상 저장 폴더
/logs/
/results/